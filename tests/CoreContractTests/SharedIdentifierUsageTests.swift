import Foundation
import XCTest

// Consumer-site compliance (verifying iOS/macOS callers use GetBoredIdentifiers
// instead of raw strings) lives in each consumer repo. This file is intentionally
// empty — getbored-core only owns schema/contract tests.
final class SharedIdentifierUsageTests: XCTestCase {}
